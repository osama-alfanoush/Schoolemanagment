services:

  nginx:
    image: nginx:1.27-alpine
    container_name: school_nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/prod.conf:/etc/nginx/conf.d/default.conf:ro
      - ./public:/var/www/public:ro
      - frontend_dist:/var/www/html:ro
      # Read-only access to uploaded files so /storage/... URLs are servable.
      - storage_data:/var/www/storage:ro
    depends_on:
      - api1
      - api2
    restart: unless-stopped
    networks:
      - school_net

  api1:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: school_api1
    env_file: .env.prod
    volumes:
      - storage_data:/var/www/storage
      # Named volume (not a host bind) so the pre-generated config/route cache is
      # shared across replicas while inheriting the image's www-data ownership —
      # required now that php-fpm runs as the non-root www-data user.
      - cache_data:/var/www/bootstrap/cache
    depends_on:
      pgbouncer:
        condition: service_healthy
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      # Application traffic is pooled; schema changes are not. The
      # pgsql_direct connection bypasses PgBouncer because transaction
      # pooling breaks session-scoped statements and CREATE INDEX
      # CONCURRENTLY must own its connection for the whole build.
      DB_HOST: pgbouncer
      DB_PORT: "6432"
      DB_DIRECT_HOST: postgres
      DB_DIRECT_PORT: "5432"
    restart: unless-stopped
    networks:
      - school_net

  api2:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: school_api2
    env_file: .env.prod
    volumes:
      - storage_data:/var/www/storage
      - cache_data:/var/www/bootstrap/cache
    depends_on:
      pgbouncer:
        condition: service_healthy
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      # Application traffic is pooled; schema changes are not. The
      # pgsql_direct connection bypasses PgBouncer because transaction
      # pooling breaks session-scoped statements and CREATE INDEX
      # CONCURRENTLY must own its connection for the whole build.
      DB_HOST: pgbouncer
      DB_PORT: "6432"
      DB_DIRECT_HOST: postgres
      DB_DIRECT_PORT: "5432"
    restart: unless-stopped
    networks:
      - school_net

  queue:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: school_queue
    # Horizon is not installed; run the plain Redis queue worker. --max-time
    # recycles the worker hourly so deploys pick up new code.
    command: php artisan queue:work redis --sleep=3 --tries=3 --timeout=90 --max-time=3600
    env_file: .env.prod
    volumes:
      - storage_data:/var/www/storage
    depends_on:
      pgbouncer:
        condition: service_healthy
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      # Application traffic is pooled; schema changes are not. The
      # pgsql_direct connection bypasses PgBouncer because transaction
      # pooling breaks session-scoped statements and CREATE INDEX
      # CONCURRENTLY must own its connection for the whole build.
      DB_HOST: pgbouncer
      DB_PORT: "6432"
      DB_DIRECT_HOST: postgres
      DB_DIRECT_PORT: "5432"
    restart: unless-stopped
    networks:
      - school_net

  scheduler:
    build:
      context: .
      dockerfile: Dockerfile.prod
    container_name: school_scheduler
    command: >
      sh -c "while true; do
        php artisan schedule:run --no-interaction;
        sleep 60;
      done"
    env_file: .env.prod
    volumes:
      - storage_data:/var/www/storage
    depends_on:
      pgbouncer:
        condition: service_healthy
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    environment:
      # Application traffic is pooled; schema changes are not. The
      # pgsql_direct connection bypasses PgBouncer because transaction
      # pooling breaks session-scoped statements and CREATE INDEX
      # CONCURRENTLY must own its connection for the whole build.
      DB_HOST: pgbouncer
      DB_PORT: "6432"
      DB_DIRECT_HOST: postgres
      DB_DIRECT_PORT: "5432"
    restart: unless-stopped
    networks:
      - school_net

  pgbouncer:
    build:
      context: ./deploy/pgbouncer
    container_name: school_pgbouncer
    env_file: .env.prod
    environment:
      DB_HOST: postgres
      DB_PORT: "5432"
      # (web replicas x PM_MAX_CHILDREN) + workers + scheduler + headroom,
      # multiplexed onto PGB_DEFAULT_POOL_SIZE real PostgreSQL backends.
      # Raise the pool size, never the server's max_connections.
      PGB_MAX_CLIENT_CONN: "${PGB_MAX_CLIENT_CONN:-500}"
      PGB_DEFAULT_POOL_SIZE: "${PGB_DEFAULT_POOL_SIZE:-25}"
      PGB_MIN_POOL_SIZE: "${PGB_MIN_POOL_SIZE:-5}"
      PGB_RESERVE_POOL_SIZE: "${PGB_RESERVE_POOL_SIZE:-5}"
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - school_net

  postgres:
    image: postgres:16-alpine
    container_name: school_postgres
    environment:
      POSTGRES_DB: school_prod
      POSTGRES_USER: school
      POSTGRES_PASSWORD: ${DB_PASSWORD:?DB_PASSWORD is required — run via deploy/prod-up.sh so it is read from .env.prod}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U school -d school_prod"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - school_net

  redis:
    image: redis:7.4-alpine
    container_name: school_redis
    command: >
      redis-server
      --requirepass ${REDIS_PASSWORD:?REDIS_PASSWORD is required — run via deploy/prod-up.sh}
      --maxmemory 512mb
      --maxmemory-policy allkeys-lru
      --save 900 1
      --save 300 10
    volumes:
      - redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "-a", "${REDIS_PASSWORD}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - school_net

  backup:
    image: postgres:16-alpine
    container_name: school_backup
    env_file: .env.prod
    environment:
      PGPASSWORD: ${DB_PASSWORD:?DB_PASSWORD is required}
      BACKUP_PREFIX: school_prod
    command: ["sh", "/ops/backup-container.sh"]
    volumes:
      - ./deploy/backup-container.sh:/ops/backup-container.sh:ro
      - ./deploy/sql:/ops/sql:ro
      - ./backups:/backups
      - storage_data:/storage:ro
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - school_net

volumes:
  postgres_data:
  redis_data:
  storage_data:
  frontend_dist:
  cache_data:

networks:
  school_net:
    driver: bridge
