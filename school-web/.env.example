# Leave empty when the SPA and API share an origin (recommended).
VITE_API_BASE_URL=/api
VITE_API_TIMEOUT_MS=15000

# Local Vite defaults to localhost. Override only for trusted LAN testing.
VITE_HOST=localhost
VITE_ALLOW_ALL_HOSTS=false
