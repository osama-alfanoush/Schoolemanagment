# Copy to .env.local to override the API base for your machine.
# .env.local is git-ignored and takes precedence over .env in every mode.
#
# Use this to talk to Laravel directly, WITHOUT the Vite dev proxy:
# VITE_API_BASE_URL=http://127.0.0.1:8000/api
