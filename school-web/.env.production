# Production build. The SPA and API are served from the same origin behind
# nginx (see laravel-api/nginx/prod.conf), which routes /api/* to PHP-FPM and
# serves the React build for everything else. A relative base keeps requests
# same-origin (no CORS, no hardcoded host in the bundle).
VITE_API_BASE_URL=/api
